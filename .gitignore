# Claude Code
.claude/
CLAUDE.md

# Python
venv/
.venv/
__pycache__/
*.pyc

# macOS
.DS_Store

# Stray weekly-agent artifacts on non-weekly branches (source lives on feature/weekly-options-agent)
options_agent/weekly/backtest/*.json

# Environment — ignore .env and every .env.<mode> secret, but keep *.example templates
.env
.env.*
!*.example

# Shadow new-golden worktree (main checkout for paper comparison)
.worktree-main/

# Runtime trade journals (generated per-mode; incl. real live-account data — never commit)
options_agent/sweet_spot_journal/
options_agent/sweet_spot_journal_live/
options_agent/sweet_spot_journal_shadow/
options_agent/sweet_spot_journal_public/

# OAuth / secrets
gmail_credentials.json
gmail_token.json
client_secret*.json

# Data cache
options_agent/data_cache/

# Real-money performance history (SQLite/CSV; account-truth data — never commit)
options_agent/perf/
